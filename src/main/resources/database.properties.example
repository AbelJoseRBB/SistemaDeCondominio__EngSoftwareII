# ============================================================
# Configuracao Local do Banco de Dados (Exemplo)
# Copie este arquivo como "database.properties" nesta mesma pasta
# e coloque a sua senha do MySQL local.
# O arquivo "database.properties" esta no .gitignore e NAO vai para o Git.
# ============================================================

db.url=jdbc:mysql://localhost:3306/condominio_db
db.user=root
db.password=sua_senha_aqui